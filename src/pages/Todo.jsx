import React from 'react';
import AuthStatus from "../api/AuthStatus";

const Todo = () => {
    return (
        <div>
            <AuthStatus/>
            Todo Page
        </div>
    );
};

export default Todo;