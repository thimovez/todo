import React from 'react';
import {Navigate} from "react-router-dom";

const PublicPage = () => {
    return (
        <div>
            Public Page
            {/*<Navigate to="/login"/>*/}
        </div>
    );
};

export default PublicPage;
